## 2024-05-18 - String Manipulation in Hot Paths\n**Learning:** String `split()` with Regex combined with array `.includes()` is extremely slow for basic validation logic, especially on frequent events like keystrokes.\n**Action:** Replaced regex-based string manipulation with reverse character traversal loops to avoid heap allocations and drastically improve performance without sacrificing readability.
